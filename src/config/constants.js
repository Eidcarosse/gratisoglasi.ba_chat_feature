/**
 * Layer: Config.
 * Shared enums and limits — the single source of truth referenced across modules.
 * e.g. MESSAGE_STATUS ('sent'), MESSAGE_TYPE ('text'|'image'|'file'),
 *      LISTING_STATUS ('active'|'sold'|'closed'), DEVICE_PLATFORM ('ios'|'android'|'web'),
 *      SOCKET_TRANSPORTS (['websocket']), and limits (max message length, page sizes,
 *      rate-limit caps). No logic — constants only.
 */
