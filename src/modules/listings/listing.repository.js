/**
 * Layer: Repository (IListingRepository — Mongo impl).
 * Data access for listings: create, getById, listBySeller, listActive, updateStatus.
 * The only place listing documents are read/written. Must NOT hold business logic.
 */
