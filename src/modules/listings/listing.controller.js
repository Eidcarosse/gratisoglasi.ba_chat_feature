/**
 * Layer: Transport (REST controller).
 * Listing HTTP endpoints (create, get, list, update status): validate input, call
 * listingService, shape the response. Must NOT hold business logic or touch the DB.
 */
