/**
 * Layer: Transport (route definitions).
 * Declares /conversations routes guarded by requireAuth, with validate() zod schemas, mapping
 * to conversation.controller handlers. Factory receiving deps from the container.
 */
