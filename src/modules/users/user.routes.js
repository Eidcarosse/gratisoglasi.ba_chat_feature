/**
 * Layer: Transport (route definitions).
 * Declares /users routes guarded by requireAuth, with validate() zod schemas, mapping to
 * user.controller handlers. Exported as a factory receiving controller/services from the container.
 */
