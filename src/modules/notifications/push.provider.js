/**
 * Layer: Service (adapter). LATER — placeholder (doc §10).
 * NOT built at MVP. Adapter over FCM (Android/web) and APNs (iOS) for sending push
 * notifications to a device token. notificationService calls this once the offline-push
 * trigger fires; until then it stays unused. Keep provider SDK details isolated here.
 */
